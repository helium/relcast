{erl_opts, [debug_info, warn_untyped_record, warnings_as_errors]}.

{cover_export_enabled, true}.
{cover_enabled, true}.
{deps, [
        %% Move back to the hex released version since that appears to
        %% include the fixes we need now
        {rocksdb, ".*", {git, "https://gitlab.com/barrel-db/erlang-rocksdb.git", {branch, "master"}}}
       ]}.

{dialyzer, [
            {warnings, [unknown]},
            {plt_apps, all_deps}
           ]}.
